---
name: build-progress-m1
description: "Build state of the VHB Super App MVP — what's scaffolded/verified and environment gotchas"
metadata: 
  node_type: memory
  type: project
  originSessionId: 7861b3d5-9956-4ad2-80e6-6761527b20a4
---

VHB Super App build started 2026-06-24 (working dir `Directus_customize_v1`). Monorepo: `backend/` (FastAPI) + `frontend/` (Next.js) + `docs/adr/`. See [[project-super-app-directus]] for stack/architecture.

**M1 COMPLETE (milestone = foundation) — all of T0–T5 done & verified 2026-06-24.**
- ✅ T0 tooling+skeleton, T1 backend, T2 frontend, T3 Postgres+Alembic, T4 auth, T5 OpenAPI-types+wiring.

**MAJOR STACK CHANGE: Supabase DROPPED** (user worried about cost + lock-in). Now: **Postgres 16 in local Docker** (`docker/docker-compose.yml`, db `vhb`/`vhb`/`vhb` on :5432) + **own FastAPI auth** (signup/login/me, app-issued HS256 JWT via `JWT_SECRET`, passwords hashed with pwdlib[argon2]). No external accounts/keys needed. See updated SPEC.md/PLAN.md + ADR 0002.
- Auth verified live end-to-end: signup→login→/me returns user; /me without/with-bad token → 401. Backend 9 pytest pass, mypy strict, ruff clean. Frontend `/login` page (login+signup, stores token in localStorage), builds clean.
- DB: Alembic async template; 1 migration applied (users table). `get_db` dependency in app/db/session.py. Models import in app/models/__init__.py for autogenerate.
- Note: logout helper (clearToken) exists but not yet wired to a UI button — trivial follow-up.
- To run: `cd docker && docker compose up -d db` (needs Docker Desktop running — it takes ~100s to start on this machine); backend `cd backend && uv run uvicorn app.main:app --reload`; frontend `cd frontend && pnpm dev`.

**Verified working:**
- Backend: FastAPI `/health` returns 200; `uv run pytest` (1 test), `ruff`, `mypy --strict` all pass. Deps via uv: fastapi, uvicorn[standard], sqlalchemy[asyncio], asyncpg, alembic, pydantic-settings, pydantic[email], pyjwt.
- Frontend: Next.js renders DashStack-themed app-shell (sidebar+topbar), `pnpm build`/`typecheck`/`lint`/`test` (vitest, 3 tests) all pass. Stack: Next 16.2.9, React 19, Tailwind 4, shadcn (base-nova style), TanStack Query, lucide-react.
- Wiring: `pnpm gen:api` generates `frontend/src/lib/api/schema.ts` from FastAPI OpenAPI; health card uses the generated type; CORS confirmed allowing localhost:3000.

**Environment gotchas (non-obvious, cost time):**
- **Next.js 16** renames `middleware` convention → **`proxy`** (matters for T4 auth route protection). Bundled docs at `frontend/node_modules/next/dist/docs/`; AGENTS.md warns Next 16 differs from training data.
- **pnpm 11**: create-next-app writes a `pnpm-workspace.yaml` with placeholder `allowBuilds: { sharp: "set this to true or false" }` that makes `pnpm install` (run before every script via verify-deps) FAIL. Fix: set `allowBuilds: { sharp: true, unrs-resolver: true }`.
- shadcn `init -d` did NOT create `src/lib/utils.ts` nor write theme tokens to globals.css — had to write `cn()` + full token set manually.
- Local machine: uv/pnpm were missing (installed: uv via astral script to ~/.local/bin, pnpm via `npm i -g`). System python is 3.9 (uv provides 3.12). Node v26, Docker present. Project root is NOT a git repo; uv init & create-next-app each created nested `.git` that were removed.

**M2/M3 partial DONE 2026-06-24** (first clickable features): Workspace + WorkspaceMember models (role owner/member, StrEnum); signup auto-creates a personal workspace (owner membership) — see services/auth.create_user. `get_current_workspace` dependency (deps/workspace.py) scopes by membership (MVP = user's first workspace). Database model + CRUD API (`/databases` GET/POST/DELETE, workspace-scoped) + `/workspaces/me`. 2nd migration applied (workspaces, workspace_members, databases). Frontend: real sidebar nav (Dashboard/Databases active; Tasks/Settings disabled "Sắp có"), `/databases` page (create/list/delete via TanStack Query mutations), dashboard shows real counts, logout button, route guard (no token or 401 → /login). 13 backend pytest pass, mypy/ruff clean, frontend build clean. Verified live: login→create CRM/Orders→list scoped.

**CRITICAL FIX — test DB isolation:** tests originally ran against the dev DB `vhb` and TRUNCATE wiped real accounts (wiped the user's admin login mid-session!). Fixed: `tests/conftest.py` now uses a SEPARATE `vhb_test` database (derived from DATABASE_URL by appending `_test`, or TEST_DATABASE_URL env), drop_all+create_all per test. **`vhb_test` database must exist** — create once: `docker exec vhb_postgres psql -U vhb -d vhb -c "CREATE DATABASE vhb_test;"`. Dev DB `vhb` is never touched by tests now.

**Seeded dev account:** admin@vhb.com / Admin12345 (recreated after the truncate incident — note: recreating gives a NEW user id, so any old JWT becomes invalid and the user must re-login).

**DATABASE ENGINE E1 DONE 2026-06-24** (the actual Notion-style engine). Field catalog locked in `docs/field-catalog.md` (25 types total, 3 phases). Key design decision: `Field.type` is **VARCHAR (Enum native_enum=False)** not a PG enum, so adding field types never needs a DB migration. number consolidates currency/percent via `options.format` (no separate currency type). status/priority = select variants; rating = number 1..5.
- **E1 implemented (13 types):** text, long_text, number(+currency/percent format), checkbox, date, url, email, phone, select, multi_select, status, priority, rating. Models `Field` + `Row` (data JSONB keyed by field id). API: `/databases/{id}/fields` GET/POST, `/fields/{id}` PATCH/DELETE, `/databases/{id}/rows` GET/POST, `/rows/{id}` PATCH/DELETE — all workspace-scoped, per-type validation in services/engine.py. 19 backend pytest pass, mypy/ruff clean.
- **Frontend Table view** at `/databases/[id]` (src/components/table/table-view.tsx + cell-editor.tsx): add field (name+type+comma-separated choices for select-like), add row, inline-edit cells per type, delete field/row, back-link + db name. Databases list items link to detail. Build/typecheck/lint clean. Verified live: create db→fields→row→inline edit.
- **Column property menu (Notion-style) DONE 2026-06-24:** `src/components/table/column-menu.tsx` — popover on column header (click or right-click) with rename + per-type "Edit property" config + delete. All field config is stored in `Field.options` (JSONB) — NO backend changes needed. Palette in `src/lib/field-colors.ts` (Notion chip colors + STATUS_GROUPS not_started/active/closed).
  - Number formats: integer/decimal(+precision)/percent/currency(+currency_code). Select/multi_select: colored tag chips + option manager (add/recolor/delete, stable crypto.randomUUID ids). Status/priority: 3 groups, add custom statuses per group + colors. URL: hyperlink toggle (opens link). Date: display format (iso/dmy/mdy/ymd). Cells render colored badges, formatted numbers, clickable URL, formatted dates (cell-editor.tsx).
  - **DEFERRED (told user, do next):** checkbox→status/select linking automation (needs backend per-row previous-state + trigger logic); date time/timezone-auto/notifications; email/phone enhancements (with People field); change-type; sort/filter/group; option drag-reorder.
- **Batch fixes/feats 2026-06-24:** (1) ROOT BUG FIXED — `apiFetch` crashed parsing JSON on 204 No Content, which silently broke ALL deletes (row/field/database); now returns undefined on 204. (2) add-field number format options corrected to integer/decimal/percent/currency. (3) ColumnMenu now renders via `createPortal`+fixed coords (was clipped by table `overflow-x-auto` when few rows). (4) Row selection: left checkbox column + select-all + selection toolbar (count / duplicate / bulk-delete / clear). (5) Copy row = bulk "Nhân bản". (6) Every new database auto-creates a default "Name" text field (in create_database). 20 backend tests pass.
  - **STILL DEFERRED (user asked, next batch):** Ctrl+Shift+Down range row selection (keyboard); drag-drop reorder of columns AND rows (needs a `Row.order` column + reorder endpoints — Row currently ordered by created_at only); Unique ID field auto-incrementing with prefix as a default field per database.
- **Table UX batch 2026-06-24:** sidebar collapse toggle (AppShell `collapsed` state + PanelLeft button in topbar; added `min-w-0` to content area so horizontal scroll works); database detail page widened to max-w-[1800px]; **column resize** (drag handle on header right edge → width stored in `field.options.width`, applied via `<colgroup>` + `table-fixed`; table width = sum of col widths so it overflows → horizontal scroll); **shift-click row range selection** (handleRowClick uses lastIndex). Still deferred: full drag-rectangle cell/column "bôi đen" selection (spreadsheet-style).
- **Reorder + unique_id + keyboard batch 2026-06-24 DONE:** `Row.seq` (immutable per-db sequence, powers unique_id) + `Row.order` (mutable drag position) added (migration with server_default='0'). `FieldType.unique_id`. **create_database now adds 2 default fields: ID (unique_id, order 0) + Name (text, order 1).** create_row assigns seq+order = max(seq)+1; list_rows orders by (order, seq). RowOut exposes `seq`. Reorder endpoints: `POST /databases/{id}/fields/reorder` and `/rows/reorder` (body {ids:[...]} → assigns order by index). Frontend: HTML5 drag-drop for column headers (drag the name button) and rows (GripVertical handle in checkbox cell); Ctrl/Cmd+Shift+ArrowDown/Up extends selection (anchor+cursor state); unique_id cell renders read-only `prefix+seq`; column-menu has prefix config for unique_id. Verified live (seq 1,2; reorder reverses). 20 backend tests pass.
- **UX polish batch 2026-06-25:** (1) custom `Dropdown`/`MultiDropdown` (`frontend/src/components/ui/dropdown.tsx`) replace ALL native `<select>` (cells select/status/priority, multi-select, add-field type/format/currency, column-menu format/currency/date). (2) No OS steppers: Rating = clickable ★ stars; Number edit = text input inputMode=decimal (no spinner). (3) All cells display-first (click to edit) → enables drag selection. (4) **Excel-style cell-range selection**: mousedown+drag highlights a rectangle, Esc clears, Ctrl/Cmd+C copies range as TSV to clipboard (skips when editing an input). (5) Floating selection toolbar (fixed bottom-center) + add-field popover (portal) — no layout push. (6) **Database UI translated to English** (table-view, column-menu, dropdown, field-colors labels, databases list); rest of app stays Vietnamese until i18n later. See [[ux-dropdown-no-native-menus]].
- **Phone + Country fields 2026-06-25:** `frontend/src/lib/countries.ts` (≈60 countries: code/name/dial + flagEmoji + parsePhone + COUNTRY_OPTIONS + DIAL_OPTIONS). Backend: added `FieldType.country` (validated as string, no migration — VARCHAR enum). **Phone** stored as string `"+84 337061976"`; PhoneCell = dial-code dropdown + number input, auto-detects code from `+84…`/`84…` paste (only when no dial set or leading `+`). **Country** stored as ISO code (`"VN"`), CountryCell = flag+name dropdown. Setting Country auto-fills phone dial codes in that row (commitCell in table-view, overridable afterward). Verified live.
- **Relation field (one-way + two-way) DONE 2026-06-25:** `RowLink` table (field_id=owner relation field, source_row_id, target_row_id; migration applied). Relation value NOT stored in Row.data — computed from RowLink and injected by `_inject_relations` in list/create/update row responses. create_field handles relation: owner field stores `options.target_database_id`+`two_way`; if two_way, auto-creates a MIRROR relation field in the target DB (`options.mirror=true, owner_field_id`) named after the source DB + sets owner's `paired_field_id`. Mirror reads links in reverse (no separate storage → always consistent). update_row/create_row `_split_relation` → `_sync_relation` (delete+recreate links for that field/row). Frontend RelationCell = MultiDropdown fetching target DB rows (label = first text field value or `#seq`); add-field popover has target-DB Dropdown + two-way checkbox. 21 backend tests pass (incl. test_relation_two_way verifying back-link). **The Formula/Rollup decision: user wants Notion-like formulas → will implement via safe asteval under the hood (NOT raw eval).**
- **Rollup field DONE 2026-06-25:** `FieldType.rollup`. `_inject_rollups` runs AFTER `_inject_relations` in list/create/update rows: reads the relation field's linked ids (already injected), fetches target rows, aggregates `options.target_field_id` by `options.function` (count/sum/avg/min/max/concat) via `_aggregate`. Read-only RollupCell on frontend; column-menu has cascading config (relation field → target DB field → function). `_split_relation` drops rollup keys (computed, never written). 22 backend tests pass (test_rollup_sum_and_count).
- **Formula field DONE 2026-06-25:** `FieldType.formula`; dep `asteval` added. `evaluate_formula(expr, lookup)` in services/engine.py uses asteval `Interpreter` with `prop("Field Name")` accessor — Notion-like, sandboxed (blocks import/exec/dunder → `__import__('os')` returns None, verified by test). `_inject_formulas` runs AFTER relations+rollups in list/create/update rows (so formulas can reference computed fields). Read-only cell (reuses RollupCell). column-menu has an Expression textarea + hint. `_split_relation` drops formula keys. 23 backend tests pass (test_formula: 10*3=30 + sandbox None). asteval has no type stubs → `# type: ignore[import-untyped]` on import.
- **Formula editor + fuller functions + Date time/tz (2026-06-25):** Expanded `evaluate_formula` (services/engine.py) with a Notion-like function library (`FORMULA_FUNCS`): logic if/and/or/not (reserved → rewritten `if(`→`_if(` via `_RESERVED_CALL` regex), empty; math round/abs/ceil/floor/sqrt/pow/mod/min/max/sum/toNumber; string concat/join/length/substring/replace/contains/lower/upper/trim/format; date now/today/dateAdd/dateSubtract/dateBetween/year/month/day/hour/minute. New endpoint `POST /databases/{id}/formula-preview` evaluates against first row → {value,type}. Frontend `formula-editor.tsx` modal (Notion-style: expression textarea + Properties list inserting `prop("Name")` at cursor + Functions reference inserting snippets + live debounced preview); opened via "Edit formula" button in column-menu. **Date field** now supports `options.include_time` (datetime-local cell), `time_format` (12/24h), `timezone` (label list; display suffix — full cross-viewer conversion still TODO). 23 backend tests pass. Notifications still deferred (with People module).
- **Per-cell date + formula error preview (2026-06-25):** Date value is now an object `{start, end|null}` (string still accepted for legacy). **Include time + End date are PER-CELL** (DateCell popover with two checkboxes + date/datetime-local inputs); column menu shows ONLY "Date format" now (time/end-date/timezone removed from column-level). So each cell independently can have time/no-time, end-date/no-end-date. Backend `validate_cell` date accepts string or `{start,end}` (validates each ISO). Display: `start → end`. Formula: `check_formula` returns (value, error); preview endpoint returns `{value,type,error}`; FormulaEditor shows red error in **Debug mode** toggle (live valid/invalid). Verified: `1+2`→3, `1 +`→SyntaxError.
- **View tools: Filter + Sort + Group + Insert L/R (2026-06-25):** `lib/view.ts` (client-side toText/toNum/operatorsFor/applyFilters/applySorts/groupRows). `view-tools.tsx` toolbar (Filter/Sort/Group buttons + portal popovers, type-aware operators + value inputs). table-view computes `visible = applySorts(applyFilters(rows))`, optional `groups`; tbody renders ungrouped or grouped (group header rows) via `renderRow(row, idx)` helper. All client-side (no backend persistence of view config yet). Column menu: **Insert left / Insert right** (creates an "Untitled" text field then reorders next to target via existing reorder endpoint).
- **View tools batch 2 (2026-06-25):** `lib/view.ts` upgraded to **nested filter tree** (`FilterGroup`/`FilterNode`/`isGroup`/`emptyGroup`/`applyFilterTree`/`countRules`) — Notion-style filter groups. `view-tools.tsx` FilterGroupEditor (recursive: Where + And/Or conj per group, Add filter rule, Add filter group, full text operators Is/Is not/Contains/Does not contain/Starts/Ends/Empty). Sort dir labels are type-aware (A→Z / 1→9 / Old→New). Group panel: Group by + Hide empty groups + Remove grouping. Group rendering in table-view: header uses `ValueChip` (exported from cell-editor → flag+name for country, colored badge for select/status/priority), header is **sticky left** (stays visible on horizontal scroll), **collapsible** (chevron toggles `collapsed` set), count shown. All client-side.
- **Freeze + Calculate + Sub-item + sort fix (2026-06-25):** Sort labels reverted to Ascending/Descending (`SORT_DIR_OPTIONS`); `applySorts` fixed — empty values always sort to the bottom regardless of direction, text compare uses `localeCompare(...{numeric:true})`. **Freeze column**: `frozenUpTo` index in table-view; `frozenStyle(colIdx)` makes columns ≤ index `position:sticky` with cumulative left offset (+ checkbox col sticky); column menu "Freeze up to here"/"Unfreeze". **Calculate**: `<tfoot>` per-column Dropdown (calcOptions: count/filled/empty/unique/%filled + sum/avg/min/max for numbers) computed over `visible` rows via `computeCalc`. **Sub-item**: backend `POST /databases/{id}/sub-items` creates two self-relation fields "Sub-item" (owner) + "Parent item" (mirror, two-way) with `options.sub_item=true`; frontend "+ Sub-items" toolbar button (hidden once enabled). Verified live (fields created). 23 backend tests pass.
- **Settings sidebar + property visibility + sub-item tree (2026-06-25):** `settings-sidebar.tsx` right drawer (gear button in toolbar) — Sub-items enable/disable toggle (moved here from toolbar) + Properties list with per-field show/hide eye toggle. table-view `hidden` Set → `displayFields = fields.filter(!hidden)` used for ALL table rendering (colgroup/header/rows/footer/frozen offsets); ViewTools still gets full `fields` (can filter/sort by hidden). Disable sub-items deletes the 2 sub_item fields. **Sub-item tree on view:** when sub-items on (and not grouped) → `treeMode`: top-level rows (empty Parent item) render with expand chevron in first cell (indented by depth), `renderTree` recurses into children (Sub-item relation ids), per-row "+ add sub-item" creates a child row + links it. Build/typecheck/lint clean.
- **Multi-page Settings sidebar (2026-06-25):** `settings-sidebar.tsx` is now a navigable panel (main → sub-pages): View (Table + "Add view" coming-soon UI), Property visibility (search + eye toggle + **drag reorder** via reorder endpoint + Hide all), Filter / Sort / Group (the editors **moved here**), Edit properties (field list → click → full per-type `FieldConfig` + delete), and Sub-items toggle on main. **`FieldConfig` extracted** to `field-config.tsx` (rename + all per-type options + formula editor) and reused by BOTH ColumnMenu and the sidebar. `view-tools.tsx` now exports `FilterGroupEditor`/`SortEditor`/`GroupEditor` (ViewTools toolbar removed). table-view toolbar = conditional Filter/Sort chips (shown only when active → open settings to that page) + gear (opens settings main). `settingsPage` state drives which page opens.
- **Search box DONE 2026-06-26 (redesigned to ALWAYS-VISIBLE bar w/ field scope, per user):** `lib/search.ts` (pure: `searchHits` 3-layer ranked exact/prefix/contains, one best hit per row; `matchedRowIds`; `cellSearchText` reuses view.ts `toText` + handles unique_id via row.seq). `search-box.tsx` exports `SearchBar` (inline, NOT a popover) sitting above the table = **field-scope `Dropdown` chip** (null = "All fields", pick a field name to search only that column — this is the "search theo tên trường" the user asked for) + keyword input + match count + "Only matches" checkbox + clear; a focus-triggered absolute dropdown (no layout push) shows suggestion chips (distinct matched values, derived from `hits`) + ranked match list with `<mark>` highlight. table-view: `searchFields = scope ? [that field] : fields`; `hits = searchHits(visible, searchFields, search)`; `visible` narrowed when filterToMatches; matched rows tinted `bg-primary/5`; clicking a match calls `jumpToRow` (scrollIntoView + 1.5s `flashId` highlight via `data-row-id`). All client-side, no backend. (Dropped earlier toggle-icon popover + `keywordSuggestions` helper.) Typecheck/lint/build clean.
- **Cell select-vs-edit UX 2026-06-26:** single click = SELECT cell only; **double-click = edit** (was click-to-edit). Impl: `editCell` state (`${rowId}:${fieldId}`) in table-view; a transparent overlay `<div absolute inset-0 z-[1]>` over each cell's editor region intercepts single clicks (so no edit) while mousedown still bubbles to `td` for range selection; overlay `onDoubleClick` sets editCell. Active cell drops the overlay → CellEditor fully interactive. `CellEditor` got `autoEdit` prop (text/number/phone open their input immediately on activate) + a remount `key={isEditing?"edit":"view"}` so state re-initializes (no setState-in-effect — Next16 lint `react-hooks/set-state-in-effect` forbids it). Esc clears editCell. unique_id cells get no overlay (read-only).
- **Native-selection off + sub-item affordance 2026-06-26:** added `select-none` to the `<table>` (kills OS text-highlight during Excel-style drag-range select; inputs get `select-text` in cell-editor inputCls so editing still selects). Sub-item add button MOVED from the far-right actions column (off-screen when scrolled horizontally) into the **Name column (colIdx 0)**: in treeMode, col 0 shows the expand chevron + a hover "+" (Add sub-item) right after it. Backend was fine all along — both owner ("Sub-item") and mirror ("Parent item") fields get `options.sub_item=True` (api/engine.py 343/357) so `treeMode` activates correctly.
- **STILL TODO (next):** real multi-view (Board/Calendar) logic; persist view config to backend (View model); per-group eye-hide list + manual group reorder.
- **Relation + Rollup + Formula trio COMPLETE.** The dynamic-DB engine now covers all 3 computed/linked field types. Remaining big epics not yet started: Filter/Sort/Group + Board/Calendar views; seed VHB data from Notion; ClickUp task module; the post-MVP roadmap (design tool, block editor, realtime, deploy).
- All clear field/table requests built (except Rollup + Formula above). Possible follow-ups if asked: Notion-style bulk field-edit from selection toolbar (image showed icon buttons); range selection currently starts on any cell mousedown (single click also highlights that cell — acceptable Excel-like behavior).
- **6 field types DONE 2026-06-26 (people, progress, created_time, created_by, last_edited_time, last_edited_by):** NO migration (Field.type is VARCHAR enum, values stored in Row.data JSONB). Backend: added the 6 to `FieldType`; validate_cell handles `people` (list of user-id strings) + `progress` (number clamped 0..100); `_AUTO_TYPES` set in api/engine.py (rollup, formula, + the 4 system types) skipped in `_split_relation` so clients can't write them; `_inject_system` reflects created_time/last_edited_time live from Row.created_at/updated_at (read-only, never stored); `_apply_auto_by` stamps created_by on insert + last_edited_by on insert&update from `current_user` (added `get_current_user` dep to create_row/update_row). New endpoint **GET /workspaces/me/members** → `MemberOut{id,email,full_name}` (powers People + by-fields display); MVP workspace has just the owner. Frontend: regen schema.ts (gen:api); cell-editor `useMembers()` (React-Query key ["members"], deduped); PeopleCell = MultiDropdown of members; UserCell (read-only name) for created_by/last_edited_by; TimeCell (read-only toLocaleString) for the *_time; ProgressCell = bar + numeric input (autoEdit-aware, clamps 0..100). Added all 6 to add-column FIELD_TYPES. 24 backend tests pass (added test_system_and_people_progress_fields), mypy/ruff clean, frontend typecheck/lint/build clean.
- **VIEWS foundation + Board DONE 2026-06-26 (user picked Notion-style multiple named views/DB):** Backend `View` model (models/view.py: ViewType table/board/calendar/gallery/gantt VARCHAR enum; `config` JSONB free-form, frontend owns shape; order) + migration `5be947580858_views` (applied to dev `vhb`). API (api/views.py, wired in main): GET/POST `/databases/{id}/views`, PATCH/DELETE `/views/{id}` — workspace-scoped via parent db. **GET lazily seeds a default "Table" view when none exist** (covers pre-existing DBs, no data backfill needed). Frontend rework: `/databases/[id]` now renders `DatabaseView` (loads views, holds activeId, renders `ViewsBar` + active renderer). `ViewsBar` = Notion-style tabs (switch / double-click rename / Trash on active / "+" type picker for all 5 types). **TableView now takes a `view` prop**: hydrates filter/sorts/group/hideEmpty/frozenUpTo/calc/hidden from `view.config` via useState initializers (remount keyed by view.id on switch → no setState-in-effect), and **persists on change via debounced (600ms) PATCH /views/{id}** (saveView mutation does NOT invalidate views query → no save→refetch loop; firstSave ref skips the mount write). Column width still lives in field.options (shared across views, not per-view). `BoardView` (board-view.tsx): Kanban grouped by a select/status/priority field (Group-by Dropdown, persisted to config.board_field), columns = choices + "No <field>", HTML5 drag card between columns → PATCH row's field value, "+ New" card per column, cards show first text field or #seq. Calendar/Gallery/Gantt = "đang phát triển" placeholder (creatable, not rendered yet). 26 backend tests pass (added test_views_crud_and_persist + isolation), mypy/ruff clean, frontend typecheck/lint/build clean.
- **Global ViewShell + Board polish 2026-06-26 (user: search+settings must show on ALL views, render once at db level "đỡ nặng"):** New `view-shell.tsx` `ViewShell` = shared chrome owning the per-view config state (filter/sorts/group/hideEmpty/frozenUpTo/calc/hidden, hydrated from view.config + debounced persist) + search state (search/scope/filterToMatches/flashId/jumpToRow + hits/matchedIds over ALL rows) + settings panel state + sub-items toggle. Renders the global toolbar (SearchBar + filter/sort chips + gear) + the active renderer + SettingsSidebar — so search & settings now appear on Board too. Exports `SharedViewProps` (the per-view state bundle). **TableView was gutted into a pure renderer**: removed its lifted state/persist/search-compute/chrome/SettingsSidebar/jumpToRow/sub-items logic; now takes `{databaseId} & SharedViewProps` (kept variable names so the body was untouched; still owns table-internal UI: editCell/selection/resize/collapse/expand/add-field/column-menu, + dbQ only for relation target picker). `DatabaseView` now renders the title/back-link (global, fixes the earlier Board gap) + ViewsBar + `<ViewShell key={active.id}>`. **BoardView polished**: applies filterRoot + search (filterToMatches→matchedIds), hides hidden fields, cards now show title + up to 3 non-hidden field previews (ValueChip for select/status/priority/country/checkbox, text otherwise), keeps group-by picker (persisted) + drag-between-columns + per-column "+ New". Frontend typecheck/lint/build clean. (No backend change this round.)
- **Board group-by-any-field + sub-group + settings 2026-06-26:** Board config (`boardField`, `boardSubgroup`) lifted into ViewShell's ViewConfig (persisted with the rest — fixes a latent bug where ViewShell's config save had no board keys and would wipe the old `board_field`). **Group by = ANY field** now: `columnsFor(field,rows)` builds columns = choices (select/status/priority), Checked/Unchecked (checkbox), or distinct `toText` values (everything else) + a "No <field>" column. **Sub-group = swimlanes**: `boardSubgroup` renders horizontal lanes (one per sub-field value), each containing the full column set; cards filtered by `matches(group) && matches(sub)`. Drag a card → sets the group field (and sub field if in a lane) via `placement()`, skipped for `NON_SETTABLE` types (rollup/formula/created_*/last_edited_*/multi_select/relation/people/unique_id). "+ New" seeds the card with the column+lane values. **Both controls moved into the Settings sidebar** (board section on main page, shown only when `viewType==='board'` — two Dropdowns listing all fields); the inline board toolbar dropdown was removed. BoardView is now a pure renderer (props: boardField/boardSubgroup/filterRoot/hidden/filterToMatches/matchedIds). Typecheck/lint/build clean.
- **CRITICAL FIX 2026-06-26 — list rows 500 (data looked "lost"):** `GET /databases/{id}/rows` 500'd (rows vanished from UI but were intact in DB) whenever a database had a **created_time/last_edited_time field together with a relation/rollup** + ≥1 row. Root cause: the `_inject_*` functions reassign `Row.data` on **session-attached** ORM rows → marks them dirty → a later inject's `db.execute` autoflushes → UPDATE expires the `updated_at` column (`onupdate=func.now()`) → `_inject_system` reading `r.updated_at` triggers an async lazy-load → `MissingGreenlet`. Fix: **detach rows (`db.expunge`) before injecting** in list_rows / create_row / update_row (read endpoints must not mutate tracked ORM objects). Regression test `test_list_rows_system_plus_relation_no_500`. 27 backend tests pass. (General rule: never reassign attributes on session-attached ORM objects in a GET path.)
- **UI polish batch (ClickUp-style) 2026-06-26:** (1) **Quick Filter/Sort/Group buttons** on the ViewShell toolbar — each opens a portal popover with the existing FilterGroupEditor/SortEditor/GroupEditor (Filter on all views; Sort+Group on table only). Gear renamed "Customize". Replaced the old active-only chips. (2) **Filter editor** widened (field w-32, op w-28, value flex) and now shown in a 520px popover instead of the cramped sidebar. (3) **Select/Status color picker** (field-config ColorPicker) was `absolute` and overlapped "Add option"/"Delete field" ("díu vào nhau") → converted to `createPortal` fixed popover anchored to the swatch. (4) **Inline "+ New" row** (ClickUp-style) at the end of the table, per group (presets the group value via `addRowInGroup`), and tree mode — removed the external dashed "New row" button. (5) **Row hover + selected-cell**: data cells made transparent (was `bg-card`) so the row's `hover:bg-muted/20` shows through; single-click selected cell (1×1 range) gets `ring-2 ring-inset ring-primary` via `isActiveCell`. Frontend typecheck/lint/build clean.
- **Top-bar restructure + in-flow Customize panel 2026-06-26 (ClickUp layout):** (1) **Search bar moved to the top bar** — `DatabaseView` now owns the search state (search/scope/filterToMatches/flashId) + computes hits/matchedIds/jumpToRow and renders the SearchBar on the **tabs row** (tabs left in a `max-w-[45%] overflow-x-auto` wrapper, search `flex-1` right). ViewsBar root is `w-max` so many tabs **scroll horizontally**. Search now persists across view switches (it's above the keyed ViewShell). (2) **Filter UI**: removed the cramped Filter/Sort/Group pages from the settings sidebar entirely (value inputs were cut off in the w-80 panel) — they live ONLY on the toolbar quick-popovers now (Filter popover is 520px so field/op/value all show). (3) **Customize panel no longer overlays the top bar**: `SettingsSidebar` converted from a `createPortal` fixed full-height overlay to an **in-flow right column** (`sticky top-2 w-80 self-start`, rendered inside ViewShell's `flex items-start` row beside the table) — it pushes the table (flex-1) and sits below the toolbar, never covering title/tabs/search/toolbar. ViewShell lost its search state/SearchBar/rows query (search props now come from DatabaseView via `shared`); SettingsPage type trimmed to main/view/visibility/fields; sidebar lost its filter/sort/group props. Frontend typecheck/lint/build clean.
- **Collapsible search + toolbar reshuffle + Quick View presets 2026-06-26:** (1) **Search collapsed to an icon** in the top bar (DatabaseView `searchOpen` state) — click → expands the SearchBar (w-24rem, right side) + an X to collapse (also clears query). Tabs are `flex-1` so search sits far right. (2) **Filter/Sort/Group buttons moved to the RIGHT** of the toolbar next to Customize (`ml-auto` group; Sort/Group still table-only). **Re-added** the Filter/Sort/Group pages to the Settings sidebar (restored props + imports + main rows + page branches that were removed last turn) — filter rule row got `flex-wrap` so it stops being cut off in the narrow w-80 panel. (3) **Quick View presets (NEW):** `Preset = {id,name,filter,sorts,group,hideEmpty}` stored in `config.presets` + `config.activePreset` (persisted via ViewShell). A Quick View `Dropdown` sits at the toolbar's LEFT (old filter/sort/group spot); selecting applies the preset's filter/sort/group; "Default view" (clear) = no preset. `dirty` = current {filter,sorts,group,hideEmpty} ≠ active preset's snapshot (or ≠ empty for Default). When dirty: **Save** button on the left (updates active preset, or names+creates a new one when on Default via inline input) + **Reset** button on the right (re-applies the baseline). (4) **Board sync:** the whole toolbar (Quick View + Filter + Customize) + sidebar render via ViewShell for every view type, so Board (and future Calendar/Gallery/Gantt) get the same chrome automatically. Frontend typecheck/lint/build clean.
- **Wrap text per field + Sort/Group/Filter in column menu 2026-06-26:** (1) **Wrap text** toggle in `FieldConfig` (shown only for text/long_text/email/url/phone) → stores `options.wrap`; `TextCell` renders `whitespace-pre-wrap break-words` (row grows) when on, else `truncate`. Appears in both the column-header popup and the settings Edit-properties field config. (2) **ColumnMenu** (the field-header popup) got a top quick-action section: Sort ascending / Sort descending (active dir highlighted), Group by this field (toggles, shows "Remove grouping" when active), Filter by this field (appends a rule to filterRoot). Wired via 3 new setters added to `SharedViewProps` (setFilterRoot/setSorts/setGroupFieldId) → TableView passes callbacks to ColumnMenu; `operatorsFor` picks the default op for the new filter rule. Frontend typecheck/lint/build clean.
- **Internal-scroll layout + search relocation + tab polish 2026-06-26:** (1) **Only the table scrolls now, not the whole page.** AppShell root → `h-screen` (definite viewport height; main stays `overflow-auto` so other pages still scroll). The DB detail page wrapper + DatabaseView + ViewShell + the table wrapper are a `flex flex-col min-h-0 flex-1` chain so the table's scroll div (`min-h-0 flex-1 overflow-auto`) gets a bounded height; **thead is `sticky top-0 z-20`, tfoot (Calculate) `sticky bottom-0 z-20`**. BoardView root likewise `flex-1 min-h-0 overflow-auto`. Settings panel changed from `sticky max-h` to `h-full`. (2) **Search moved back to the toolbar next to Customize** — DatabaseView builds the collapsible search element (icon → expands to SearchBar+X, pop-out preserved) and passes it as a `searchSlot` ReactNode prop into ViewShell, which renders it just before the Customize button. Search state stays in DatabaseView (persists across views). (3) **Tabs cleaned up (ClickUp-style)** in ViewsBar: `px-3 py-2`, active tab uses an absolute rounded underline bar (`bg-foreground`) instead of border-b-2, `whitespace-nowrap`, and the add button is now "+ View". Frontend typecheck/lint/build clean. (Watch: border-collapse + sticky thead can drop the header's bottom-border in some browsers — bg distinguishes it; revisit if needed.)
- **View management in settings + sticky add-row 2026-06-26:** Backend: new `POST /databases/{id}/views/reorder` (mirrors fields/rows reorder). Frontend: (1) settings main row "Layout / View" → **"Layout"**; its page is now a **layout-type switcher** (lists the 5 types; Table/Board switchable via `PATCH /views/{id}{type}`, others "soon"). (2) New **"Views" management page** in settings: lists all views with drag-reorder (`reorderViews`), inline rename (double-click → PATCH name), **duplicate** (POST copy of name/type/config, stays on current view), **set as default** (Star → reorder to front; default = first view, DatabaseView already picks `views[0]`), and **delete with inline confirm** (disabled when only 1 view). Needs new props `viewId/activeId/setActiveId` (DatabaseView→ViewShell→SettingsSidebar). (3) **Removed the Trash from the tab bar** (misclick-prone) — delete is now only in settings, with confirm. (4) **Add-row is now sticky**: moved out of tbody into the sticky `<tfoot>` as a clickable "New" row above Calculate (shown when not grouped); **the whole row is clickable** (tr onClick), same for per-group add rows. (5) **Stronger row hover** (`hover:bg-muted/60`). 27 backend tests pass; frontend typecheck/lint/build clean.
- **Pagination + scroll/sticky fixes + column hide/delete-confirm 2026-06-26:** (1) **Load limit** (per-view, persisted in `config.limit`, default 10; options 10/20/50/100/200) — Dropdown on the settings **Layout** page (`limit`/`setLimit` threaded DatabaseView→ViewShell→SettingsSidebar; added to ViewConfig + SharedViewProps). (2) **Client-side pagination/windowing**: TableView renders `visible.slice(0, shown)` (shown init = limit); a **"Load more (N left)"** row sits in the sticky tfoot right below the New row, `+= limit` per click. Only for the flat (non-group, non-tree) list. This is the right call given filter/sort/group are client-side (server-side LIMIT/OFFSET would break them) — and rendering fewer DOM rows is also what makes scroll smoother. (3) **Header/footer were showing content through them** — root cause: `<thead>`/`<tfoot>` cells used semi-transparent `bg-muted/40` & `/20`, so scrolled rows bled through the sticky bars. Fix: added solid `bg-card` to the `<thead>` and `<tfoot>` ELEMENTS (cells keep their tint on top → opaque). (4) **Over-scroll fixed** with `overscroll-contain` + `[scrollbar-gutter:stable]` on the table scroll div. (5) **Hide column** action (EyeOff) added to the ColumnMenu (adds field to `hidden` via new `setHidden` in SharedViewProps). (6) **Delete column now confirms** inline (Cancel/Delete) in the ColumnMenu. Frontend typecheck/lint/build clean. (Future: true server-side pagination needs filter/sort moved to the backend.)
- **Pagination/calc/scroll round 2 2026-06-26:** (1) **Load limit moved to the main Settings page** (was on Layout) and **now actually works** — replaced the `shown` useState (which never reacted to limit changes) with `pages` state + derived `shown = limit*(pages+1)`, so changing the dropdown re-windows immediately. (2) **Bottom bar redesigned**: a single sticky `<tfoot>` row (only when not grouped) with **"+ New" pinned left** (`sticky left-0 bg-card`, so it never scrolls out horizontally) and **"Load more (N left)" as a bordered button pinned right** (`sticky right-0`, hover). (3) **Over-scroll fully killed** — `overscroll-none` (not just `-contain`) on the table scroll div AND on AppShell `<main>`. (4) **Calculate moved off the table**: removed the per-column tfoot Calculate dropdown; now each column's calc function is set in the **ColumnMenu** (a "Calculate" Dropdown, calcOptions+calcValue+onCalc passed from TableView); the computed values render in a **summary bar BELOW the table** (`shrink-0`, so the table flex-1 shrinks up to make room), **bold**, showing **only columns that have a calc configured** (`calcFields = displayFields.filter(f => calc[f.id])`, computed over the full filtered `visible`). Frontend typecheck/lint/build clean.
- **Popup placement/z-index fixes 2026-06-26:** (1) **Column field-settings menu now opens on LEFT-click only** — removed the header's `onContextMenu` (right-click) trigger that positioned the menu at the cursor (often low + behind the sticky New footer). (2) **ColumnMenu raised above the sticky footer**: overlay z-30→z-40, panel z-40→z-50 (footer is z-30); also clamps `maxHeight = vh - y - 16` so a tall field config never runs off-screen behind the footer. (3) **Cell dropdowns flip up near the bottom**: shared `Dropdown`/`MultiDropdown` (ui/dropdown) Panel now opens upward (`bottom`-anchored from the trigger's top) when `innerHeight - trigger.bottom < 264` and there's more room above — fixes select/status/relation popups in bottom rows getting hidden behind the footer / off the viewport. Frontend typecheck/lint/build clean.
- **Cell-edit UX + bulk add + sub-item pagination + freeze borders 2026-06-26:** (1) **Cell editing is now 1-click-select / click-again-or-double-click-edit**: overlay got `onMouseDown` (captures `clickedActive.current = isActiveCell()` BEFORE the td sets the range) + `onClick` (edits if the cell was already selected) + `onDoubleClick` (edits directly). Select-like cells now **auto-open** their dropdown on edit — added `autoOpen` to `Dropdown`/`MultiDropdown` (ref-callback that opens once, lint-safe; not a useEffect) and passed `autoOpen={autoEdit}` from SelectCell/MultiCell/CountryCell/RelationCell. So text→input, select→open menu, all on the edit action (no extra click). (2) **Bottom bar reordered**: a sticky-left `w-max bg-card` group = **Load more (left, ChevronDown icon)** + **New (bordered button)** + **bulk-add dropdown** (`BulkAddRows`: number input ≤100 → confirm → `bulkAdd` mutation hitting the **new backend `POST /databases/{id}/rows/bulk` {count:1..100}** endpoint). (3) **Load limit now works in tree (sub-item) mode**: top-level rows window by `shown` (`topCount = treeMode ? topLevel.length : visible.length`); each parent **preloads only 5 children** (`childShown[rowId] ?? 5`) with a per-parent **"Load more sub-items (N left)"** row that adds `limit` more. (4) **Freeze-column borders fixed**: border-collapse drops sticky-cell borders, so `frozenStyle`/`checkboxFrozen` now redraw them via `box-shadow` (inset right+bottom borders for inner frozen cols; the last frozen col gets a `4px 0 6px -3px rgba(0,0,0,.25)` divider). 15 engine tests pass (added test_bulk_create_rows); frontend typecheck/lint/build clean.
- **Sub-item auto-name + wrap-all-fields 2026-06-26:** (1) **Adding a sub-item now drops straight into its title input** — `addSubRow` finds the first text/long_text field and `setEditCell(`${child.id}:${titleField.id}`)` after the parent PATCH + invalidate, so the user types the name immediately (was: had to hunt for the double-click). This was the "subitem không gõ được tên" report — render path was identical to top-level, the friction was no auto-focus on add. (2) **Wrap text toggle now shows on ALL fields** (field-config: gate changed from a text-type whitelist to `field.type !== "unique_id"`). Made wrap actually render: `displayCls` in cell-editor is now a `(field) =>` fn that swaps `truncate`↔`whitespace-pre-wrap break-words` per `field.options.wrap` (threaded `field` into PhoneCell/UserCell/TimeCell); TextCell already self-read wrap; the table-view `<td>` drops `overflow-hidden` when wrap so grown rows aren't clipped. Skipped chip-wrapping for multi_select/people (separate ask). typecheck/lint/build clean.
- **VIEWS still TODO:** Calendar / Gallery / Gantt renderers; per-view column width; click card → row detail modal; Board card sort; persist activeView per user; manual view reorder.
- **E2 remaining:** invite flow — ⚠️ **NO Invite model exists** (verified 2026-06-28, earlier note "Invite model exists" was WRONG); must build model + invite + accept from scratch. **E3 remaining:** files(Storage), location(Maps) — both need new infra, deferred. Seed VHB from Notion still pending.
- **State verified 2026-06-28:** branch `feature/26.06.03` git CLEAN (06-27 WIP committed @ 37ef833/9dbf847). Backend models = database/field/user/view/workspace (5, NO invite). Routers = auth/databases/engine/views/workspaces. Frontend routes = `/`, `/databases`, `/databases/[id]`, `/login` (no Docs/Tasks). View renderers (2026-06-29) — ALL 6 implemented, no Table fallback: table, board, **gantt** (gantt-view.tsx + gantt-scale.ts, tab "Timeline"), **list** (list-view.tsx), **calendar** (calendar-view.tsx, day/4days/week/month/year + now-line + drag-reschedule), **gallery** (gallery-view.tsx). Added `list` to backend ViewType enum (VARCHAR, no migration) + regen schema. List/Gallery take `{databaseId} & SharedViewProps` (filter/sort/group/search/limit, editable title via CellEditor, inline prop chips, Load more + New). Topbar Filter/Sort/Group shows on ALL views (Sort→Board via applySorts; Board Group→boardField).
- **feature/26.06.05 → main + QA + changelog (2026-06-29/30):** Committed the full recovery + all 6 layouts as `d995b70` on `feature/26.06.05`, pushed, then **force-pushed to replace `main`** (dropping the broken `f33d7c3`) — `feature/26.06.05` is now the official main. QA'd the built layouts live via the connected Chrome extension (user logged in manually; agent refused to auto-enter the test password admin@vhb.com/Admin12345 per safety rule — always let the user authenticate). Bugs found+fixed: Calendar Week/4days/Day horizontal overflow (missing `min-w-0` on flex day cells) → added to header/all-day/hour cells; Calendar Year showed today red on trailing days of adjacent month → gated `today = inMonth && sameDay(...)`; Board card + List/Gallery rendered date fields as `[object Object]`/raw ISO → added `displayText(field,value)` to `lib/view.ts` (formats `start → end`, locale, keeps time when present) and used it everywhere `toText`/`String(v)` was shown to the user; added a live time badge in the Calendar hour-gutter next to the now-line. **Drag-drop QA**: Gantt bar drag (mouse-event based) verified working end-to-end (dragged a real row, deadline updated + list re-sorted). Board/Calendar use native HTML5 drag-and-drop (`draggable`+`onDragStart`/`onDrop`) which the browser-automation tool cannot simulate — those still need a manual drag test by the user. **Changelog**: the two old `CHANGELOG-2026-06-26.md`/`-27.md` files had been lost from HEAD during the force-push recovery (existed only in git history at commit `37ef833`); retrieved their content via `git show` and merged everything into a single root `CHANGELOG.md` (newest section on top: 2026-06-29 official-main entry, then 06-27 recovery, then 06-26 features). As of this write, `CHANGELOG.md` + the QA polish fixes above are **uncommitted in the working tree** on `feature/26.06.05` — offered to commit + re-push main, awaiting user confirmation.
- **Pending/next work (discussed with user 2026-06-29, not yet started):** MVP-blocking — Invite+Accept model (no Invite model exists at all yet), Roles/Permissions, seed real VHB data from Notion (F1), E2E Playwright tests + acceptance-criteria review (G1), i18n (app is Vietnamese except the DB engine which was deliberately done in English), responsive/mobile layout. Post-MVP epics (not started) — ClickUp-style Tasks module, Docs/block editor, realtime sync, file storage, a design tool, production deploy. Known perf debt — filter/sort/pagination are all **client-side** (fetches every row); should move server-side before real data volume hits. Cloud hosting was discussed (sized for ~40 concurrent internal users + ~50 concurrent public visitors) but no infra has been provisioned yet — this is a recommendation only, not a decision made.
- **Gantt features (2026-06-28):** two-row ClickUp header (minor cols + major group via unitTicks/unitLabel); Time period = Giờ/Ngày/Tuần/2tuần/Tháng/Quý/Năm (PERIODS: minor/major/dayPx/windowDays). Window centered on today ±windowDays, sticky "Tải thêm ‹/›" extend buttons. Axis field = date/created_time/last_edited_time/date-returning formula (looksDate). Freeze panel = Name + date col + extra fields, resizable (drag). Bars draggable to move/resize ONLY when axis is a writable `date` field (applyDrag → PATCH row). Per-row 🎯 jump-to-bar, red today line, weekend shading (hour/day), row load-limit + New row. Gantt toolbar (Today/Timeline by/Time period) is portaled into ViewShell's toolbar slot (`view.type==="gantt"`). **RECOVERY 2026-06-29:** commit "Gantt View Refine" (9154d59) had committed raw merge-conflict markers into view-shell/settings-sidebar/board-view (a botched stash-pop mixing two architectures). Fixed by: restoring view-shell.tsx + settings-sidebar.tsx from clean parent 953ec16 (the "upstream" arch where **ViewShell owns search internally + signature is `{databaseId, view}`**, matching the still-clean table-view/database-view), keeping current board-view, re-integrating Gantt, then rebuilding the lost toolbar features. **Current ViewShell arch (authoritative):** owns search (SearchBar) internally; toolbar = Quick-View preset dropdown + Save/Reset + gantt-toolbar slot + SearchBar + quick Filter/Sort/Group popovers + Customize gear. Table/Board/Gantt all paginate via `limit` (default 10 from config, no size-picker UI yet) + "Load more"/"New" footer (unified style). **2026-06-29 update:** search ownership MOVED to DatabaseView (owns search/scope/filterToMatches/flashId + computes hits/matchedIds + renders SearchBar on the Layout/tabs row, right side, w-80). ViewShell now takes `views/activeId/setActiveId/search/filterToMatches/matchedIds/flashId` props (no longer owns search). DatabaseView also owns fields/rows queries for search. SettingsSidebar got: Rows-per-page (limit) dropdown, **Layout** page (reorder/rename/duplicate/delete/set-default views via PATCH `order` — added `order` to backend ViewUpdate), **View presets** page. ColumnMenu got quick Sort/Group/Filter/Wrap-text. Table: internal vertical scroll (max-h + sticky thead/tfoot), pagination (limit + Load more/New), cell UX = click select / click-again-or-dbl-click edit (Dropdown `autoOpen`). DateCell = Notion-style calendar picker (chips, month nav, Today, range, End date/Include time toggles, Date-format dropdown persists field.options.date_format). ViewsBar: delete removed, tabs drag-reorder. View config keys: ganttField, ganttScale, ganttLeftFields, ganttColWidths, ganttDateFormat, presets, activePreset, limit. Load more/New UI unified across table/board/gantt. Bottom **unscheduled tray** (undated rows, shares axis, synced horizontal scroll, click/drag to set date → row moves to timeline). Domain **auto-fits to all loaded dated bars** (today±window grown to data; Load-more rows auto-extends timeline). Side "More" buttons only when scrolled to an edge. Hour period drags/sets by hour (ymdhm); else by day (ymd). Date display format configurable in Customize sidebar (DATE_FORMATS, exported from gantt-view). MVP gap = 3 real items: F1 seed VHB, Invite+accept, G1 E2E/AC tests.
